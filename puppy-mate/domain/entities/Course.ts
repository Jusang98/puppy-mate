export class Course {
    constructor(
        id: number,
        userId: number,
        name: string,
        courseImageUrl: string,
        address: string,
        isPublic: boolean,
        distance: number,
        duration: number,
        createdAt: Date,
        updatedAt: Date
    ) {}
}
